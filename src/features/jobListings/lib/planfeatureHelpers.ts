// All features are now FREE - no plan restrictions!

export async function hasReachedMaxPublishedJobListings() {
  // Always return false - unlimited job listings for everyone!
  return false
}

export async function hasReachedMaxFeaturedJobListings() {
  // Always return false - unlimited featured listings for everyone!
  return false
}

